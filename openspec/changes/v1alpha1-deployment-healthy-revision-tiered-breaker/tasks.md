## 1. API 与模型收敛

- [ ] 1.1 定义 v1alpha1 仅支持 Deployment 的 CRD 字段约束与校验规则（验收：非 Deployment 配置被拒绝；失败路径返回明确错误）
- [ ] 1.2 扩展 status 字段以表达策略阶段、回滚目标、对象级/域级熔断状态（验收：状态迁移完整可追踪；失败路径写入原因）
- [ ] 1.3 同步更新 Helm Chart values 约束与默认值（验收：values 可渲染且与 CRD 字段一致；失败路径触发校验失败）
- [ ] 1.4 编写 API 单元测试（默认值、枚举、边界）覆盖上述变更（验收：通过 go test；失败路径断言错误信息）

## 2. 编排主链路与适配接口

- [ ] 2.1 在 Reconciler 中建立“安全门禁→快照→L1/L2/L3→审计回写”的统一编排骨架（验收：重复事件幂等；失败路径可回滚）
- [ ] 2.2 定义最小 WorkloadAdapter 接口并实现 Deployment 适配器（验收：编排层不直接依赖具体 workload 细节）
- [ ] 2.3 加入对象类型保护（仅 Deployment 可写操作）与审计事件（验收：非 Deployment 仅告警不写入）
- [ ] 2.4 编写编排层单元测试（幂等、错误传播、非支持类型）与并发 race 测试（验收：go test -race 通过）

## 3. 最近健康 Revision 回滚能力

- [ ] 3.1 实现 Revision 历史检索与健康候选筛选（按时间逆序）（验收：可选中最近健康 Revision；失败路径跳过不健康候选）
- [ ] 3.2 实现“无健康 Revision”降级到 L3 人工介入（验收：禁止 L2 写操作并触发高优先级告警）
- [ ] 3.3 实现回滚前快照校验与回滚后状态确认（验收：回滚失败可恢复到安全状态）
- [ ] 3.4 编写回滚模块单元测试（正常/缺数据/全不健康/回滚失败）与边界测试（验收：测试覆盖主要失败路径）

## 4. 双层熔断与安全门禁

- [ ] 4.1 实现对象级熔断计数、冷却与恢复条件（验收：达到阈值后暂停对象自动动作）
- [ ] 4.2 实现域级熔断（命名空间或全局）及优先级覆盖规则（验收：域级生效时对象级恢复也不可绕过）
- [ ] 4.3 接入维护窗口、速率限制、爆炸半径边界判定（验收：命中门禁时仅只读评估+告警）
- [ ] 4.4 编写安全门禁测试（时间窗口、速率限制、爆炸半径边界值）和熔断恢复测试（验收：边界用例全部通过）

## 5. 可观测性与审计闭环

- [ ] 5.1 定义并接入关键指标（触发次数、成功率、回滚次数、熔断次数、耗时）（验收：指标可按策略级别聚合）
- [ ] 5.2 输出结构化日志与 K8s Event（含触发源、目标、前后状态、结果）（验收：可按资源与策略检索）
- [ ] 5.3 定义最小告警集（失败率异常、熔断触发、维护窗口冲突）（验收：告警路由到位且可去重）
- [ ] 5.4 编写可观测性单元测试（指标打点、事件字段完整性）（验收：失败路径同样产生日志与事件）

## 6. 质量门禁与交付

- [ ] 6.1 固化 CI 检查：gofmt/goimports、go vet、golangci-lint、go test、go test -race（验收：任一失败阻断合并）
- [ ] 6.2 增加 CRD/API 生成物一致性检查（如 controller-gen）（验收：生成物漂移时 CI 失败）
- [ ] 6.3 建立代码评审清单（代码规范、测试充分性、可回滚性、可观测性）（验收：PR 必填项齐全）
- [ ] 6.4 补充发布与回滚说明（灰度启用策略、风险与应急步骤）（验收：文档可直接用于发布评审）
