## 1. 配置驱动化收敛

- [ ] 1.1 清理编排链路中的硬编码阈值与窗口，统一从 `HealingRequest.Spec` 读取（验收：执行路径不再出现固定业务阈值；失败路径返回参数缺失/非法错误）
- [ ] 1.2 将门禁评估参数与 `ApplyDefaults` 对齐并补充边界校验（验收：默认值、最小值、越界值可断言；失败路径可定位到字段）
- [ ] 1.3 同步更新 Helm `values.yaml` 与 `values.schema.json` 约束（验收：渲染与 schema 校验通过；不合法配置被拒绝）
- [ ] 1.4 补充配置驱动单元测试（验收：覆盖幂等窗口、速率限制、爆炸半径、熔断阈值边界与错误分支）

## 2. 运行态门禁输入事实化

- [ ] 2.1 为编排层引入运行态输入提供器（动作计数、影响 Pod、集群 Pod 总数）（验收：门禁判定使用运行态值；失败路径进入只读阻断）
- [ ] 2.2 将维护窗口/限频/爆炸半径判定改为事实输入驱动（验收：命中门禁时仅只读评估+告警；未命中可继续执行）
- [ ] 2.3 增加时间窗口、限频、爆炸半径边界值测试（验收：边界值全部可复现并可断言）
- [ ] 2.4 增加门禁失败审计与事件证据断言（验收：失败路径包含关联键、门禁原因、输入摘要）

## 3. Deployment 回滚适配器生产化

- [ ] 3.1 实现 Deployment/ReplicaSet Revision 历史读取与候选构建（验收：可按时间逆序返回候选；无数据路径可诊断）
- [ ] 3.2 实现回滚执行动作并对接快照恢复语义（验收：回滚成功更新状态；回滚失败触发恢复并输出证据）
- [ ] 3.3 将“证据不足→L3”与运维建议输出绑定真实候选数据（验收：候选为空/证据不足时禁止 L2 且给出建议）
- [ ] 3.4 补充回滚链路单元测试（验收：正常、候选为空、执行失败、恢复失败路径均可断言）

## 4. 熔断与幂等参数贯通

- [ ] 4.1 使对象级/域级熔断阈值与冷却时间由请求配置驱动（验收：不同阈值配置产生可预测触发结果）
- [ ] 4.2 将 Webhook 幂等窗口从固定值改为请求参数化窗口（验收：非默认窗口下重复抑制行为正确）
- [ ] 4.3 输出熔断触发证据（层级、阈值、计数、恢复时间）到状态/事件/审计（验收：可直接定位触发依据）
- [ ] 4.4 补充熔断优先级与恢复边界测试（验收：域级优先、对象恢复不绕过域级约束）

## 5. 可观测性生产输出

- [ ] 5.1 将关键指标接入可采集实现（触发次数、成功率、回滚次数、熔断次数、策略耗时）（验收：Prometheus 可抓取且标签维度受控）
- [ ] 5.2 将运行时事件接入 K8s Event 输出并保留结构化日志（验收：可按资源和策略级别检索）
- [ ] 5.3 保持 audit/event/metric 的关联键一致性（验收：单次请求可通过关联键串联全链路）
- [ ] 5.4 增加可观测性测试（验收：成功与失败路径均产出完整观测证据）

## 6. 质量门禁与交付验证

- [ ] 6.1 执行并通过 `go test ./...` 与关键失败路径测试（验收：核心策略模块失败路径覆盖）
- [ ] 6.2 执行并通过 `go test -race ./internal/...`（验收：controllers 与策略链路无竞态告警）
- [ ] 6.3 执行并通过 `go vet ./...`、`golangci-lint run`、CRD 一致性检查（验收：任一失败阻断交付）
- [ ] 6.4 更新发布与回滚文档（风险、灰度启用、回退步骤）（验收：文档可直接用于发布评审）
