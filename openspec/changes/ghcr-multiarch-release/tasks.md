## 1. 多架构构建与发布链路

- [ ] 1.1 将 Dockerfile 调整为平台感知构建，移除当前写死的 amd64 目标，并确保同一源码可为 linux/amd64 与 linux/arm64 输出正确二进制。
- [ ] 1.2 新增独立的 GitHub Actions GHCR 发布工作流，仅接受 `v` 前缀语义化版本 tag push 作为正式发布入口，并保留受控的 `workflow_dispatch` 入口用于预发布验证或补发。
- [ ] 1.3 在发布工作流中固化稳定版、预发布版与 `latest` 的标签判定逻辑，确保稳定版 `vX.Y.Z` 更新 `latest`，预发布标签不覆盖 `latest`。
- [ ] 1.4 为发布链路增加可验证检查，确认推送结果包含 `linux/amd64` 与 `linux/arm64` 两种 manifest，并覆盖“非正式标签不得触发正式发布”的失败路径。

## 2. 官方镜像消费入口

- [ ] 2.1 将官方运行镜像名称收敛到 `ghcr.io/YangYuS8/kube-sentinel`，并评估 README、安装说明、脚本默认说明与 chart 使用示例中的镜像引用是否需要同步更新。
- [ ] 2.2 更新本地部署与联调文档，显式区分“本地 build + minikube image load”路径与“直接消费官方 GHCR 镜像”路径，明确两者适用场景。
- [ ] 2.3 在安装与部署说明中补充稳定版标签、预发布标签和 `latest` 的使用边界，避免将三类镜像入口混写为同一种推荐方式。

## 3. 验证与回归

- [ ] 3.1 为镜像标签判定逻辑补充自动化测试或等价校验步骤，至少覆盖稳定版、预发布版和非法标签三类输入。
- [ ] 3.2 为多架构发布结果补充回归验证步骤，确认 amd64 与 arm64 环境都能通过同一镜像引用拉取匹配产物，并记录失败时的诊断方式。
- [ ] 3.3 执行并记录与本变更直接相关的质量检查，至少包括工作流静态校验、文档/脚本引用一致性检查，以及本地开发回路未被官方镜像引入破坏的回归验证。
