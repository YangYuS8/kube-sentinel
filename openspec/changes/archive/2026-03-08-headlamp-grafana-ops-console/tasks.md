## 1. 对象视图可读性

- [x] 1.1 为 `HealingRequest` 补齐适合 Kubernetes 原生控制台消费的展示元数据，例如打印列或等价列表展示约束，使 Headlamp/通用控制台能直接识别阶段、最近动作与关键阻断语义。
- [x] 1.2 收敛 `HealingRequest.status` 中供控制台消费的关键字段，确保阶段、最近动作、失败或阻断原因、下一步建议和 `correlationKey` 语义稳定且可验证。
- [x] 1.3 为上述对象视图语义补充对应的 API/CRD 测试，至少覆盖列表展示元数据存在性、关键状态字段完整性和关联键稳定性。

## 2. 指标视图接入

- [x] 2.1 为控制器 metrics 提供标准 Prometheus 抓取入口，并在 Helm/chart 或安装清单中定义最小监控接入配置，避免仅依赖手工抓取说明。
- [x] 2.2 为 Grafana 定义最小 dashboard 输入与分组，至少覆盖总体触发与成功率、策略执行结果、快照结果和门禁趋势。
- [x] 2.3 为指标暴露和 dashboard 输入补充验证步骤或自动化测试，至少覆盖抓取入口存在性、关键指标分组和失败路径诊断提示。

## 3. 文档与联调回路

- [x] 3.1 更新本地与测试环境文档，明确说明如何分别启用 Headlamp 对象视图和 Grafana 指标视图，以及各自的前置条件。
- [x] 3.2 在本地开发回路说明中区分“对象视图验证”和“指标视图验证”，避免把控制台联调与控制器功能验证混写。
- [x] 3.3 执行并记录与本变更直接相关的质量检查，至少包括 CRD/状态语义验证、监控接入配置检查和文档入口一致性检查。
