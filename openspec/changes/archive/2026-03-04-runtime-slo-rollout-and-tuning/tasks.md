## 1. 灰度启用与阈值调优流程

- [x] 1.1 实现 SLO 分层灰度启用控制（环境/命名空间分层）并定义每层进入与回退条件；验收标准：仅当上一层稳定窗口通过时允许进入下一层，回退条件命中可立即降级。
- [x] 1.2 实现阈值调优审批与回滚机制（记录审批信息、保留上一版本阈值）；验收标准：无审批记录时阈值更新被拒绝，回滚可恢复到上一次有效配置。
- [x] 1.3 为灰度与调优流程补充单元测试和边界测试（空层级、非法阈值、观察窗口内重复调优）；验收标准：异常输入均被阻断，合法流程可稳定通过。

## 2. 门禁与值班联动增强

- [x] 2.1 在质量门禁输出中补齐语义级别映射（`allow/info`、`degrade/warning`、`block/critical`）；验收标准：每次门禁结果都附带可解析告警级别。
- [x] 2.2 实现同对象同级别告警抑制与去重策略；验收标准：抑制窗口内重复告警不重复通知，且保留抑制计数。
- [x] 2.3 为门禁与值班映射增加幂等与回滚测试（重复事件、状态抖动、恢复条件未满足）；验收标准：重复输入结果一致，未满足恢复条件时不提前放量。

## 3. 闭环演练与复盘可审计

- [x] 3.1 扩展闭环演练脚本覆盖“灰度放量 -> 越线回退 -> 阈值调优 -> 恢复观察”全路径；验收标准：每个阶段都有可检索证据输出并可自动断言。
- [x] 3.2 增加“质量门禁语义 + SLO 治理语义 + 值班级别”一致性断言；验收标准：任一语义冲突即判定失败并阻断验收。
- [x] 3.3 增加复盘字段校验（越线原因、处置动作、阈值调整结论、后续观察计划）；验收标准：缺失任一关键字段时演练失败。

## 4. 文档与最终验收收口

- [x] 4.1 更新运行手册与发布回滚文档，补充灰度策略、调优节奏、值班抑制规则与回滚步骤；验收标准：值班人员可按文档独立完成一次越线处置。
- [x] 4.2 更新告警规则最小集与抑制窗口说明；验收标准：告警规则与门禁动作映射一致，warning/critical 行为差异清晰。
- [x] 4.3 执行并记录最终验收：`go test ./...`、关键路径 `go test -race`、`go vet ./...`、`golangci-lint run`、CRD 一致性与质量门禁；验收标准：全部通过且变更可进入 `/opsx:apply`。
