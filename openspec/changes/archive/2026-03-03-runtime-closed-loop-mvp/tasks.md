## 1. 事件入口与请求映射

- [x] 1.1 实现 Alertmanager Webhook Receiver 与统一事件模型映射（验收：合法事件可创建/更新 HealingRequest；失败路径返回可诊断错误）
- [x] 1.2 实现事件幂等去重键与重复请求抑制逻辑（验收：幂等窗口内重复事件不触发重复写操作）
- [x] 1.3 增加非 Deployment 目标的入口拒绝与只读输出（验收：非支持类型不触发写操作且保留审计证据）
- [x] 1.4 编写入口层单元测试（合法/缺字段/重复事件/非Deployment）（验收：覆盖成功与失败路径）

## 2. 运行时闭环编排

- [x] 2.1 将事件驱动接入 Reconciler 主链路（门禁→快照→L1/L2/L3→状态回写）（验收：单次演练可完成完整状态流转）
- [x] 2.2 扩展状态字段与事件输出以包含关联键、阶段与错误原因（验收：status/event/审计可通过关联键串联）
- [x] 2.3 落实无健康 Revision 证据不足与 L3 降级输出（验收：候选为空或证据不足时禁止L2并进入L3）
- [x] 2.4 编写编排链路单元测试（阶段推进、降级分支、幂等重放）（验收：关键失败路径全部可断言）

## 3. 熔断与安全门禁运行化

- [x] 3.1 实现对象级与域级熔断运行态计数与优先级约束（验收：域级生效时对象恢复不可绕过）
- [x] 3.2 输出熔断触发证据（阈值、计数、恢复时间）到状态/事件/审计（验收：触发后可直接定位原因）
- [x] 3.3 接入维护窗口、速率限制、爆炸半径的运行态判定（验收：命中门禁时仅只读评估+告警）
- [x] 3.4 编写安全门禁测试（时间边界、限频边界、熔断恢复边界）（验收：边界值用例通过）

## 4. 闭环验收与可观测性

- [x] 4.1 建立主线A演练脚本（触发事件→执行链路→输出断言）（验收：可重复执行且结果一致）
- [x] 4.2 增加三项强断言检查：仅Deployment写、无健康Revision降级L3、双层熔断阻断（验收：断言失败可明确定位）
- [x] 4.3 对齐指标与告警：失败率异常、熔断触发、维护窗口冲突（验收：告警规则可触发且可去重）
- [x] 4.4 编写可观测性测试（指标打点、事件字段、审计关联键）（验收：成功与失败路径均有完整输出）

## 5. 工程质量与交付

- [x] 5.1 同步更新 CRD/API 与 Helm values.yaml 约束（验收：schema 与 values 一致且可渲染）
- [x] 5.2 固化 CI 检查（go test、go test -race、go vet、golangci-lint、CRD一致性）（验收：任一失败阻断合并）
- [x] 5.3 补充发布与回滚说明（闭环灰度启用、异常降级步骤、风险说明）（验收：文档可用于发布评审）
- [x] 5.4 增加主线A最小验收清单文档（输入条件、预期行为、失败路径）（验收：可作为上线门禁清单）
