## ADDED Requirements

### 需求:Deployment L1 首发统一门禁裁决

系统必须在首发版本中将维护窗口、速率限制、爆炸半径和熔断统一视为 Deployment L1 的写前裁决条件；任一条件不满足时必须阻断写操作并输出唯一裁决结果。

#### 场景: 任一门禁失败

- **当** Deployment 首发 L1 同时或分别命中维护窗口、速率限制、爆炸半径或熔断阻断
- **那么** 系统必须阻断写动作并输出可检索的最终裁决原因
