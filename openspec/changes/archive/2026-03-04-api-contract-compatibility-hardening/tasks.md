## 1. API 契约治理落地

- [x] 1.1 梳理并补齐 `api/v1alpha1` 关键字段的默认值、可选性与校验约束声明；验收标准：字段约束清单可追溯且存在边界值定义。
- [x] 1.2 实现 API 兼容性分类模型（backward-compatible / migration-required / version-bump-required）及结构化输出；验收标准：每次 API 变更都可映射唯一兼容性分类。
- [x] 1.3 实现 status 最小语义集合校验（phase、lastAction、failureReason、nextRecommendation）；验收标准：缺失任一语义字段时必须阻断契约校验。

## 2. 门禁与发布判定联动

- [x] 2.1 在统一质量门禁中增加 API/CRD/Helm 一致性检查项；验收标准：三者不一致时输出 `block` 与可执行修复建议。
- [x] 2.2 在质量门禁输出中增加兼容性证据字段（分类、受影响字段、迁移要求、风险等级）；验收标准：每次门禁执行均输出可解析证据。
- [x] 2.3 将生产门禁判定绑定 API 契约风险；验收标准：高风险或迁移条件未满足时发布流程被阻断。

## 3. 测试与回归覆盖

- [x] 3.1 新增 API 契约治理单元测试（非法默认值、约束缺失、无效分类）；验收标准：异常输入全部失败并给出明确原因。
- [x] 3.2 新增 status 语义边界测试（字段缺失、空值、兼容映射）；验收标准：缺失与空值路径均被阻断，兼容映射路径通过。
- [x] 3.3 新增门禁幂等/回滚测试（重复输入、状态抖动、迁移条件未满足）；验收标准：重复输入结果一致，未满足条件时不得提前放量。
- [x] 3.4 补充 Helm 约束同步测试（values/schema 与 API 契约一致性）；验收标准：Helm 未同步时 CI 和质量门禁均阻断。

## 4. 交付收口与最终验收

- [x] 4.1 更新运行手册与发布回滚文档，补充兼容性分类解释、迁移路径模板与阻断处置流程；验收标准：值班人员可按文档独立完成一次兼容性阻断处置。
- [x] 4.2 更新 CI 与质量门禁证据说明，明确 API 契约校验在发布判定中的优先级；验收标准：证据字段与发布结论可一一对应。
- [x] 4.3 执行最终验收：`go test ./...`、关键路径 `go test -race`、`go vet ./...`、`golangci-lint run`、CRD 一致性与质量门禁；验收标准：全部通过且变更可进入 `/opsx:apply`。
