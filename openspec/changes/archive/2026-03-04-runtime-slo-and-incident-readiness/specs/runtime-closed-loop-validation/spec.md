## ADDED Requirements

### 需求:SLO 越线到响应升级闭环断言
系统必须在闭环演练中验证 SLO 越线后从门禁判定到响应升级的完整链路。

#### 场景: 越线触发升级链路
- **当** 演练注入 SLO 越线条件
- **那么** 系统必须输出门禁结果、升级等级与建议动作的完整证据

### 需求:恢复前置条件闭环验证
系统必须在闭环演练中断言恢复前置条件的满足状态；未满足时不得标记恢复完成。

#### 场景: 恢复条件未满足
- **当** 演练中恢复前置条件缺失
- **那么** 系统必须保持降级或阻断状态并报告缺失项

### 需求:值班可诊断性输出断言
系统必须验证值班输出至少包含门禁类别、根因提示、修复建议与推荐 runbook 标识。

#### 场景: 值班输出字段不完整
- **当** 演练生成响应建议但缺少关键字段
- **那么** 系统必须判定闭环验收失败
