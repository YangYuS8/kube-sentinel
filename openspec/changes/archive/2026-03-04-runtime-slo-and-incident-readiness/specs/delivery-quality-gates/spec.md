## ADDED Requirements

### 需求:门禁结果关联 SLO 状态输出

质量门禁必须输出与 SLO 状态关联的结果字段，至少包含当前动作等级、预算状态与建议响应级别。

#### 场景: 门禁完成输出 SLO 关联字段

- **当** 统一质量门禁完成一次评估
- **那么** 系统必须输出可解析的 SLO 关联结果字段

### 需求:门禁与治理状态一致性

系统必须校验质量门禁结果与 SLO 治理结果语义一致；若不一致必须返回阻断结论并提示修复。

#### 场景: 门禁与治理语义冲突

- **当** 质量门禁输出与 SLO 治理判定不一致
- **那么** 系统必须阻断交付并输出一致性修复建议
