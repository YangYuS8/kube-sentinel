## 1. 证据门槛与状态机收敛

- [x] 1.1 为 L2 增加三位一体证据校验链路（回滚可达性/告警稳定窗口/依赖完整性）（验收：任一证据失败必须禁止 L2 并降级 L3，状态与事件可检索）
- [x] 1.2 在编排状态机中引入 `PendingVerify` 与 `Suppressed` 语义（验收：观察期内恢复事件仅抑制不写操作）
- [x] 1.3 为 `PendingVerify`/`Suppressed` 与证据不足降级补充单元测试（验收：覆盖成功、抑制、降级三类路径）
- [x] 1.4 为证据链失败路径补充审计与事件字段断言测试（验收：关联键、阻断原因、证据摘要完整）

## 2. 告警观察期分级矩阵

- [x] 2.1 实现按告警类别/严重度选择 Soak Time 的策略表（验收：CrashLoop/OOM/Probe/Pending 四类可配置且可命中）
- [x] 2.2 支持观察期采样稳定性判定（最小连续样本约束）（验收：瞬时抖动不触发写操作）
- [x] 2.3 为分级观察期与采样判定补充边界值测试（验收：窗口临界点与样本临界点行为一致）

## 3. 分层只读阻断预算

- [x] 3.1 实现命名空间预算按应用数计算：`UnhealthyWorkloads/TotalWorkloadsInNamespace`（验收：达到阈值触发 namespace 只读阻断）
- [x] 3.2 实现最小基数保护与绝对值回退阈值（验收：小样本 namespace 不因比例失真误阻断）
- [x] 3.3 实现预算阻断优先级（namespace 预算优先于对象级自动动作）（验收：双命中时统一进入只读阻断）
- [x] 3.4 为预算触发与优先级补充单元测试（验收：比例阈值、绝对值阈值、优先级三类场景覆盖）

## 4. 影子执行与白名单应急机制

- [x] 4.1 在阻断路径输出影子执行说明（拟执行动作/阻断原因/证据摘要/预算命中信息）（验收：status、event、audit 三处信息一致）
- [x] 4.2 设计并实现 `criticality: high` 受限紧急尝试权（验收：仅允许一次受控尝试，失败后立即回归阻断）
- [x] 4.3 为白名单尝试权补充幂等性与回滚性测试（验收：重复触发不绕过限制，失败路径可恢复）

## 5. API/Chart/观测契约对齐

- [x] 5.1 同步 CRD 字段与状态字段约束（观察期、阻断原因、证据摘要、预算占比）（验收：默认值/范围/枚举校验通过）
- [x] 5.2 同步 Helm `values.yaml` 与 `values.schema.json` 新增参数（观察期矩阵、预算阈值、最小基数、白名单开关）（验收：schema 拒绝非法配置）
- [x] 5.3 补充 Prometheus/K8s Event 指标与标签（阻断类型、证据缺失、抑制结果）（验收：可通过关联键串联）
- [x] 5.4 为 API/Chart/观测变更补充回归测试（验收：序列化、默认值、指标输出路径通过）

## 6. 质量门禁与交付验证

- [x] 6.1 执行并通过 `go test ./...`（验收：关键策略模块含失败路径测试）
- [x] 6.2 执行并通过 `go test -race ./internal/...`（验收：controllers 与策略链路无竞态告警）
- [x] 6.3 执行并通过 `go vet ./...`、`golangci-lint run`、CRD 一致性检查（验收：任一失败阻断交付）
- [x] 6.4 更新发布与回滚文档（保守模式灰度策略、阻断预算、应急回退）（验收：文档可用于发布评审）
