## 1. 门禁判定契约与优先级

- [x] 1.1 在 `internal/safety` 增加统一门禁判定器（allow/block/degrade）与固定优先级（维护/安全 > 熔断 > 发布阈值），并新增单元测试覆盖单次判定唯一结果与多门禁并发命中裁决；验收：输入并发命中样例时仅输出一个结果+原因码，失败路径返回默认保守阻断。
- [x] 1.2 定义标准化门禁原因码与阈值快照数据结构（含样本窗口、关键指标、下一步建议），并新增序列化/反序列化单元测试；验收：输入完整/缺失字段样例时结构化输出可检索，失败路径必须标记数据完整性不足。
- [x] 1.3 实现门禁越线自动降级动作（只读评估、告警、人工介入建议）与幂等保护，并新增幂等性与回滚性单元测试；验收：重复触发降级不产生重复写操作，失败路径可回退到上一安全状态。

## 2. 与 Deployment 分层与 L2 回滚联动

- [x] 2.1 在 `internal/healing/orchestrator.go` 对齐 Deployment 三阶段输出，补充阶段迁移与门禁判定关联证据写入，并新增单元测试覆盖 L1 成功、L1->L2、L2->L3 路径；验收：每次阶段迁移都带门禁结果与原因码，失败路径保留可追溯审计记录。
- [x] 2.2 在 `internal/healing/rollback.go` 将 L2 回滚结果接入门禁权重计算，支持连续窗口失败比例阻断，并新增边界值单元测试（窗口长度、失败比例阈值）；验收：阈值内允许、越线阻断并给出降级建议，失败路径触发快照恢复证据。
- [x] 2.3 在 `internal/healing/snapshot.go` 补充回滚失败后的恢复结果与门禁影响说明输出，并新增单元测试验证恢复成功/失败两条路径；验收：恢复结果可关联到同次门禁评估，失败路径返回人工介入建议。

## 3. 熔断冲突裁决与安全语义

- [x] 3.1 在 `internal/safety/circuit_breaker.go` 实现熔断与发布门禁并发命中的统一裁决来源输出，并新增单元测试覆盖对象级/命名空间级并发场景；验收：并发命中时输出固定优先级与最终动作，失败路径默认命名空间只读阻断。
- [x] 3.2 在 `internal/safety/gates.go` 强化命名空间预算、冷却时间、速率限制的边界检查，并新增边界值单元测试（最小/最大窗口、阈值临界点、爆炸半径）；验收：临界值行为与配置一致，失败路径拒绝不合法配置并给出原因码。
- [x] 3.3 为门禁/熔断/降级动作补充回滚策略（撤销临时状态、恢复评估模式）并新增回滚性单元测试；验收：回滚后状态与前置快照一致，失败路径不允许继续自动升级动作。

## 4. 可观测性、演练与验收矩阵

- [x] 4.1 在 `internal/observability/metrics.go` 与 `internal/observability/audit.go` 输出门禁判定报告（结果、越线项、恢复条件、建议动作），并新增单元测试校验必填字段完整性；验收：灰度窗口结束可检索完整报告，失败路径标记缺失字段并阻断放行。
- [x] 4.2 更新 `scripts/drill-runtime-closed-loop.sh` 增加 allow/block/degrade 三类路径演练断言，并为脚本关键解析逻辑补充单元测试（或表驱动测试）；验收：预生产演练必须覆盖三类结果，失败路径输出明确断言失败点。
- [x] 4.3 新增/扩展闭环验证测试（`internal/healing/orchestrator_test.go`、`internal/observability/observability_test.go`）以验证证据链完整性与语义一致性；验收：阶段路径、门禁原因码、降级动作三者可一致关联，失败路径保留调试上下文。

## 5. API/Helm 约束与交付收口

- [x] 5.1 若新增门禁策略字段，更新 API 类型与状态证据字段（`api/v1alpha1`）并补充单元测试验证默认值与兼容性；验收：旧配置可无损运行，新字段缺省时保持保守默认，失败路径拒绝非法组合。
- [x] 5.2 同步更新 Helm 参数约束（`charts/kube-sentinel/values.yaml` 与 `values.schema.json`）并新增 schema 校验测试；验收：门禁阈值/窗口/速率限制可被正确校验，失败路径在安装或渲染阶段报错。
- [x] 5.3 执行回归验证（`go test ./...`、关键演练脚本、CRD/Chart 一致性检查）并记录发布门禁验收结论；验收：无新增失败用例且演练结论明确为 allow/block/degrade 之一，失败路径输出回退与人工介入建议。
