## 1. 最小安装入口

- [ ] 1.1 为测试环境补齐 Kube-Sentinel 的最小安装入口（如 Helm 模板或等价安装封装），覆盖 CRD、控制器部署与默认配置加载；验收标准：新集群可通过统一入口完成安装，并具备继续执行 smoke 的提示输出。
- [ ] 1.2 为最小安装入口补充验证用例与同步检查；验收标准：相关模板/脚本具备至少一条可自动化验证路径，且不会破坏现有 CRD 与 chart 默认值约束。

## 2. 本地开发回路

- [ ] 2.1 新增本地开发入口，统一环境自检、CRD 安装、manager 启动或连接、demo workload 准备；验收标准：在 kube context、CRD、端口或集群基础组件未就绪时能够明确阻断并输出修复提示。
- [ ] 2.2 为本地开发入口补充测试或脚本化验证方案；验收标准：关键失败路径覆盖 kube context 缺失、CRD 缺失、端口占用与基础组件未就绪等场景。

## 3. 本地 smoke 验证

- [ ] 3.1 收敛本地 smoke 验证入口，固定默认阻断路径与放宽单次测试请求后的成功路径；验收标准：同一测试环境可重复验证 HealingRequest 创建、默认 gate/block 语义与 PendingVerify -> Completed 成功闭环。
- [ ] 3.2 为 smoke 回路补充边界与幂等验证；验收标准：重复执行同一 smoke 不会产生不可解释的残留状态，且小集群 blast radius 处理方式有明确输出说明。

## 4. 文档与 minikube 体验

- [ ] 4.1 更新 README、联调清单与相关说明文档，明确统一安装入口、本地开发入口与 minikube 代理前提；验收标准：新开发者可按文档完成安装、自检和 smoke。
- [ ] 4.2 明确小规模集群下 blast radius 的测试约定与恢复要求；验收标准：文档区分本地临时放宽与生产保守默认值，避免误用到正式环境。

## 5. 质量门禁与回归验证

- [ ] 5.1 运行并通过本变更相关的自动化检查；验收标准：`go test ./...`、`make quality-gate`、CRD 一致性检查以及本地 minikube smoke 验证通过，并记录失败路径与修复建议。
