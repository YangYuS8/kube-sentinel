## ADDED Requirements

### 需求:首发 L1 写前快照阻断

系统必须将 Deployment 首发 L1 的持久快照创建视为硬性前置条件；快照创建失败时禁止任何自动写动作继续执行。

#### 场景: 快照失败阻断 Deployment L1

- **当** Deployment 首发 L1 在写动作前创建持久快照失败
- **那么** 系统必须阻断 L1 写动作并记录快照失败原因、阶段和建议动作
