## ADDED Requirements

### 需求:交付证据文件强约束输出
统一质量门禁必须输出标准化交付证据文件，至少包含门禁结论、失败分类、关键原因码与修复提示。

#### 场景: 门禁执行后输出证据
- **当** 质量门禁完成一次执行
- **那么** 系统必须生成可解析的交付证据文件并包含最小必填字段

### 需求:证据缺失阻断发布
当交付证据缺失关键字段或格式不合法时，系统必须阻断后续发布流程。

#### 场景: 证据字段缺失
- **当** 证据文件缺失门禁结论或原因码字段
- **那么** 系统必须返回阻断结果并提示补齐证据
