## ADDED Requirements

### 需求:预生产 dry-run 自动执行
系统必须在交付流水线中自动执行预生产 dry-run，并覆盖关键运行路径验证。

#### 场景: 触发预生产 dry-run
- **当** 发布前流水线进入验证阶段
- **那么** 系统必须自动执行 dry-run 并返回结构化结果

### 需求:三态门禁覆盖断言
dry-run 必须覆盖 allow/degrade/block 三类门禁语义，任一缺失时必须判定验证失败。

#### 场景: 缺失门禁路径覆盖
- **当** dry-run 结果未覆盖 allow/degrade/block 中任意一类
- **那么** 系统必须标记验证失败并阻断发布推进
