## 1. 统一质量门禁入口

- [ ] 1.1 在 `Makefile` 增加统一质量门禁入口（例如 `quality-gate`）并按固定顺序串联 `go test ./...`、核心路径 `go test -race`、`go vet ./...`、`golangci-lint run`、CRD 一致性检查；验收标准：本地一条命令可执行全部检查，任一检查失败即非零退出。
- [ ] 1.2 为统一入口补充单元/脚本测试（至少覆盖“全部通过”“某一步失败即阻断”）；验收标准：测试可断言执行顺序与失败短路语义。
- [ ] 1.3 在 CI workflow 中切换到统一入口并保留最小日志摘要；验收标准：CI 与本地检查口径一致，失败时可直接定位失败步骤。

## 2. CRD 同步阻断与诊断输出

- [ ] 2.1 强化 `scripts/check-crd-consistency.sh` 的失败分类输出（类别、根因提示、修复建议）；验收标准：CRD 漂移时输出结构化提示并返回阻断状态。
- [ ] 2.2 为 CRD 一致性脚本增加测试夹具（匹配/不匹配两类输入）；验收标准：测试覆盖成功路径与失败路径，失败路径包含修复提示断言。
- [ ] 2.3 在涉及 API 变更的流程中补充 Helm 约束同步检查（`values.yaml` 与 `values.schema.json`）；验收标准：API/CRD/Helm 任一不同步时门禁阻断。

## 3. 发布门禁绑定与闭环验收

- [ ] 3.1 在发布/演练脚本中接入质量门禁结论并实现 `allow`/`block`/`degrade` 三态判定；验收标准：`block` 时禁止发布推进，`degrade` 时执行保守降级路径。
- [ ] 3.2 为三态判定补充单元测试与边界测试（含空结果、未知类别、重复执行幂等）；验收标准：幂等执行结果稳定且异常输入可回退到安全默认值。
- [ ] 3.3 在闭环验收脚本中增加“预提交与 CI 语义一致性”断言；验收标准：同一提交在两侧结论不一致时必须判定为失败。

## 4. 文档与验收收口

- [ ] 4.1 更新运行手册与排障文档，给出统一门禁命令、常见失败分类和修复路径；验收标准：文档可独立指导完成一次失败修复闭环。
- [ ] 4.2 增补演练脚本输出样例与验收矩阵说明；验收标准：能从输出中定位门禁类别、根因提示、修复建议。
- [ ] 4.3 执行并记录最终验收：`go test ./...`、关键包 `-race`、`go vet`、`golangci-lint`、CRD 一致性；验收标准：全部通过且变更可进入 `/opsx:apply`。
