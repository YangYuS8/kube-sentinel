## 1. 编排器分层扩展

- [ ] 1.1 在 `internal/healing/orchestrator.go` 为 Deployment 引入显式 L1/L2/L3 状态机与阶段迁移条件（验收：给定 L1 失败输入会进入 L2，给定 L2 不可执行输入会进入 L3；失败路径输出阶段原因）
- [ ] 1.2 为 Deployment 分层迁移补充单元测试（验收：覆盖成功迁移、熔断阻断、重复触发幂等拒绝 3 条路径）

## 2. L2 健康回滚与证据

- [ ] 2.1 扩展 Deployment L2 健康候选筛选与依赖校验逻辑（验收：仅选择满足健康窗口与依赖约束的 revision；候选不足时降级 L3）
- [ ] 2.2 补充 Deployment 回滚审计与证据输出字段（验收：输出候选选择依据、执行结果、下一步建议；失败路径含不可回滚原因）
- [ ] 2.3 为 L2 逻辑增加幂等性与回滚性测试（验收：重复触发不产生二次写入；回滚失败可恢复到安全停止状态）

## 3. 熔断联动与发布门禁

- [ ] 3.1 将 Deployment L1/L2 写动作接入对象级与域级熔断判断（验收：熔断打开时仅执行只读评估与告警，禁止自动写操作）
- [ ] 3.2 增加 Deployment 分层指标与发布阻断判定（验收：L1 成功率、L2 成功率、L3 降级率可观测；阈值越线触发阻断）
- [ ] 3.3 为熔断阈值、冷却时间、升级阈值补充边界值测试（验收：覆盖最小值、阈值临界点、超阈值三类输入）

## 4. 配置与交付一致性

- [ ] 4.1 将 Deployment 分层升级阈值与阻断条件参数化并接入配置读取（验收：删除硬编码值，配置缺失时使用受控默认并记录告警）
- [ ] 4.2 同步 Helm Chart 配置约束（`charts/kube-sentinel/values.yaml` 与 `charts/kube-sentinel/values.schema.json`）（验收：新增字段通过 schema 校验，非法输入被拒绝）

## 5. 验证与回归

- [ ] 5.1 增补运行时闭环验证用例，形成 Deployment 三阶段验收矩阵（验收：覆盖 L1 成功、L1→L2、L1→L3 三条主流程）
- [ ] 5.2 执行并记录质量门禁（`go test ./...`、`go test -race ./...`、`go vet ./...`、`make lint`）（验收：与本变更相关模块全部通过，失败项具备可复现日志）